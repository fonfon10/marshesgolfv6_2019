<%= form_with(model: day, local: true) do |form| %>
  <% if day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
      <% day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
<%= @day.name.strftime("%A/%d-%^b-%y") %>  </div>


  <div class="field">
    <%= form.label :open_close %>
    <%= form.select(:open_close_id, @open_closes)%>
    
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
